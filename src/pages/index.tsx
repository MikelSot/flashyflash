import { useAuthContext } from '@modules/Home/provider/Auth.provider'
import type { NextPage } from 'next'
import { useRouter } from 'next/router'

const Home: NextPage = () => {
  return <h1>inicio sesion</h1>
}

export default Home
